# Live banner updates (scripts/fetch_data.py)
python-dateutil>=2.8
requests>=2.28
lxml>=4.9

# SYSTEM.INFO YAML (optional - scripts fall back to a tiny parser)
PyYAML>=6.0

# Portrait generation from photo (scripts/image_to_ascii.py)
numpy>=1.24
Pillow>=10.0
